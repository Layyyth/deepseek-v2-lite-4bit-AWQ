1- create the directory
cd workflow
mkdir awq-project
cd awq-project
touch quantize.py

2- start a venv
python -m venv myenv
source myenv/bin/activate


3- install nvim & libraries 
sudo apt update && sudo apt upgrade -y
sudo apt install neovim -y

git clone https://github.com/mit-han-lab/llm-awq.git
cd llm-awq
pip install -e .

pip install transformers datasets torch accelerate safetensors

4- run quantize script



